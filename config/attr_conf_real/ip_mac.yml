# Specification of attributes generated by "IP-MAC mapper" input module
entity:
  id: ip_mac
  name: IP-MAC
  description: IP address and MAC address mapping.
  key_data_type: string
  auto_create_record: true

attribs:
  a:
    name: IP-MAC mapping
    description: Time intervals in which given mapping was true.
    data_type: tag
    history: true
    history_params:
      pre_validity: 24h
      post_validity: 24h
      max_age: 14d

